# JSON outputs (Phase 7A)

One JSON per `test_id` from Phase 6 inference.

**Naming:** Prefer `{test_id}.json` when batch script exists; until then Phase 6 default is `{audio_stem}.json`.

**Generated after:** You record P0 audio and run `explain_prediction.py` per manifest row.
