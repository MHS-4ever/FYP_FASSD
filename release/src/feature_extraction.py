"""Acoustic feature extraction skeleton compatible with Phase 8C style."""

from __future__ import annotations


def extract_acoustic_features(audio_waveform, sample_rate: int):
    # TODO(Phase 9C): wire a compact, release-safe wrapper around accepted feature set.
    # Intentionally does not duplicate all research code paths in Phase 9A.
    return {"status": "placeholder", "feature_set": "acoustic_phase8c_style", "vector": None}
